[project]
name = "daum-market-guard"
version = "0.1.0"
description = "Daum Cafe used-market image reuse monitor for PC and Raspberry Pi."
requires-python = ">=3.11"
dependencies = [
  "playwright>=1.45.0",
  "Pillow>=10.4.0",
]

[project.scripts]
daum-market-guard = "daum_market_guard.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
